# =============================================================================
# Kubernetes Service Account for Medical Chatbot
# =============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: medical-chatbot-sa
  namespace: medical-chatbot
  labels:
    app: medical-chatbot
automountServiceAccountToken: false
